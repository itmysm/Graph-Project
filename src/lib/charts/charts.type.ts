export type ChartOptionType = "keyVal" | "flat"

function makeResultReadableForCharts(res, type) {
    // every thing is fine now and you show get the result and make the readable for charts! the problem is that few have few type of charts and also few type of results you should engaded these variableties togather!~
}